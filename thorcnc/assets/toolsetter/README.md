Place hover images here. Filenames are referenced from
`SettingsTabModule._TS_HOVER_MAP` in `thorcnc/modules/settings_tab.py`.

Recommended size: 800x600 px (will be scaled to fit, aspect ratio kept).
PNG or JPG.

Expected files:
- change_position.png   — manual tool change safety position
- probe_position.png    — sensor XY/Z location
- large_tools.png       — large tool diameter offset from contact surface
- spindle_zero.png      — distance between spindle nut and toolsetter
- max_probe.png         — maximum downward probing distance
- retract.png           — retract distance after first touch
- search_vel.png        — fast pre-positioning speed
- probe_vel.png         — slow fine-measurement speed
- ts_before.png         — before-toolsetter macro
- ts_after.png          — after-toolsetter macro
- default.png           — fallback when no widget is hovered (optional)
